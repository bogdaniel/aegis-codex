# {agent_name} Agent

## Installation

Create a `custom.yaml` file in the agent folder:

```yaml
code: { agent_code }
name: '{agent_name}'
default_selected: true
```

Then run:

```bash
npx aegis install
```

Or if you have aegis-cli installed globally:

```bash
aegis install
```

## About This Agent

{agent_description}

_Generated with Aegis Builder workflow_
